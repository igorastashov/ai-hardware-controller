# Инструкция для AI-агента

> Ты автономный агент-разработчик, работающий в этом репозитории.
> Этот файл — твоя точка входа. Прочитай его целиком перед любым действием.

## Главная карта

Полная документация, правила ведения проекта и структура папок описаны в **`docs/index.md`**.
Прочитай этот файл перед началом любой задачи.

## Архитектура

Верхнеуровневая архитектура, слои и допустимые зависимости описаны в **`ARCHITECTURE.md`**.

## Аксиомы проекта

Фундаментальные принципы, определяющие стиль и подход, описаны в **`docs/design-docs/core-beliefs.md`**.

## Critical Non-Negotiables (BLOCKER)

Эти пункты обязательны к явному подтверждению в первом ответе после ознакомления с репозиторием. Пропуск любого пункта = ошибка исполнения.

1. **`BLOCKER: Commit Harness`**  
   Микро-коммиты и коммиты по триггерам (step / harness-fix / harness / wip) с корректным форматом сообщения.
2. **`BLOCKER: Docs First`**  
   Перед задачей прочитай `docs/index.md`, затем релевантные разделы.
3. **`BLOCKER: Verify Before Finish`**  
   Перед завершением запускай `scripts/verify.sh` (Windows/PowerShell: `powershell -File scripts/verify.ps1`).
4. **`BLOCKER: Exec Plan Discipline`**  
   Для задач сложнее 2 коммитов веди `Exec Plan` в `docs/exec-plans/active/`.
5. **`BLOCKER: Multi-Agent Safety`**  
   Не редактируй чужие активные планы, не используй `git stash`, не читай `completed/` целиком.
6. **`BLOCKER: Immutable Core Protection`**  
   Не удаляй/не переименовывай файлы каркаса без явного подтверждения человека.

## Протокол первого ответа (обязателен)

После чтения этого файла агент обязан дать короткое подтверждение в формате чеклиста:

- [x] Commit Harness принят (микро-коммиты + триггеры + формат).
- [x] Docs First принят (`docs/index.md` вначале).
- [x] Exec Plan и Surprises & Discoveries приняты.
- [x] Verify Before Finish принят (`scripts/verify.sh`; Windows: `powershell -File scripts/verify.ps1`).
- [x] Ограничения multi-agent/immutable core приняты.

Если любой пункт не подтвержден явно, ответ считается неполным и должен быть перегенерирован.

## Обязательные правила (нарушение = провал задачи)

1. **Читай карту перед работой.** Начни с `docs/index.md`. Найди нужный раздел, спустись к деталям. Не гадай — читай документацию.

2. **Создавай Exec Plan для задач сложнее 2 коммитов.** Скопируй `docs/exec-plans/TEMPLATE.md` в `docs/exec-plans/active/<имя-задачи>.md`. Веди его по ходу работы.

3. **Записывай сюрпризы.** Если обнаружил неожиданное поведение — запиши в секцию `Surprises & Discoveries` своего Exec Plan. По возможности — закрепи тестом.

4. **Запускай verify перед завершением.** Выполни `scripts/verify.sh` (Windows/PowerShell: `powershell -File scripts/verify.ps1`) и убедись, что ошибок нет.

5. **Не читай `/completed/` целиком.** Папка `docs/exec-plans/completed/` — архив. Используй семантический поиск (Semantic Search) или grep для нахождения нужного контекста. Никогда не перебирай все файлы.

6. **Один план = одна ветка = один агент.** Не редактируй чужие активные планы в `docs/exec-plans/active/`.

7. **Не удаляй файлы каркаса.** Список защищённых файлов указан в `docs/index.md`, секция B.1. Если задача требует их удаления — остановись и запроси подтверждение у человека.

8. **Не используй `git stash`.** Другие агенты могут работать параллельно.

## Правила фиксации (Commit Harness)

В agent-first среде исправления стоят дешево, а потеря контекста — дорого. Ты должен фиксировать состояние (создавать git commit) по жестким триггерам:
1. **Шаг плана (step):** Выполнил один пункт из `Exec Plan` (обновил чекбокс + написал код) -> commit.
2. **Зеленый линтер (harness/fix):** Запустил `scripts/verify.sh` (или на Windows `powershell -File scripts/verify.ps1`) и получил 0 ошибок -> commit.
3. **Сюрприз (harness):** Написал тест/скрипт, фиксирующий неожиданное поведение -> commit.
4. **Пауза (wip):** Перед делегированием человеку или долгой паузой -> commit.

Формат коммита: `<тип>: <суть изменения> [ссылка-на-шаг-плана]`.

## Self-check перед каждым ответом

Перед отправкой ответа проверь:

1. Не потеряны ли `BLOCKER`-правила при сокращении текста.
2. Есть ли явное упоминание критичных ограничений, если они релевантны текущему шагу.
3. Не заменил ли я обязательное правило на расплывчатую формулировку.
4. Если сомневаюсь в приоритете правила — трактую как более строгий уровень и отмечаю это в ответе.

## Где искать информацию

| Что нужно | Где искать |
|---|---|
| Как устроен проект | `ARCHITECTURE.md` |
| Правила и процессы | `docs/index.md` |
| Принципы «вкуса» | `docs/design-docs/core-beliefs.md` |
| Проектные решения (RFC) | `docs/design-docs/` |
| Текущие задачи | `docs/exec-plans/active/` |
| История решений (Context Graph) | `docs/exec-plans/completed/` (через поиск!) |
| Продуктовые требования | `docs/product-specs/` |
| Шпаргалки по стеку | `docs/references/` |
| Описание скриптов | `scripts/index.md` |
