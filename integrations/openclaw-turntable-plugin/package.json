{
  "name": "@ai-hardware-controller/openclaw-turntable-plugin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "External OpenClaw plugin for Revopoint turntable tools.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "smoke": "node --enable-source-maps dist/src/smoke.js",
    "smoke:dev": "vitest run tests/smoke-entrypoint.test.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
